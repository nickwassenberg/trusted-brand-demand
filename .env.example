# Copy this file to .env and fill in your key.
# The app also accepts the API key directly in the UI, so this is optional.

ANTHROPIC_API_KEY=sk-ant-...

# Optional: change the port (default: 5000)
# PORT=5000
